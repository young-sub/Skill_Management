# Codex `/goal` Contract

## Objective

## Issue or tracker reference

## Confirmed purpose and observable changes

## Implementation Contract

- Purpose:
- Core implementation target:
- Observable changes:
- Non-goals:
- Verification signal:

## Scoped Overrides

- <override or none>

## Source-of-truth docs to read

## Proposed Shared Doc Updates to respect

- Blocking before implementation:
- To apply during implementation:
- To defer to close/next:

## Decisions made

## Out of scope

## Vertical slice order

1. <slice>
2. <slice>

## TDD strategy

- Main-owned RED/GREEN acceptance:
- Delegated verification use: <none | focused tests | broad suite | failure triage | CI log review>
- Model class: <fast/small allowed | main/high required | inherit default>
- Allowed model use:
- Forbidden decisions:
- Evidence required per cycle:

## Verification loop

- Direct checks:
- Delegated checks:
- Evidence capsule required: `agent`, `cwd`, `branch/ref`, `git status`, `command`, `exit code`, `duration`, `expected result`, `actual summary`, `failure signature`, `file:line`, `artifact/log path`, `confidence`, `recommended next action`
- Rerun budget and diagnose trigger:

## Diagnostics expectations

## Scoped architecture review target

## Documentation updates

## Stop conditions

Include stopping if the implementation would violate the confirmed purpose, observable changes, non-goals, scoped overrides, or shared-doc reconciliation policy.

## Final report format

Include implemented slices, changed files, exact verification evidence, docs updated, proposed shared-doc updates applied/deferred/rejected, architecture review result, remaining risks, and unverified assumptions.
