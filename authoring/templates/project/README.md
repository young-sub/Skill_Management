# Project Templates

WP-02 owns the concrete `AGENTS.md`, `CLAUDE.md`, `TESTING.md`, and `.harness/project.yaml` templates. Add them here only with fixture-backed bootstrap behavior and an explicit resource-map target.
