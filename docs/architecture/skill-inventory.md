# Public Skill Inventory

Harness v3 public Skills are `setup-agent-harness`, `explore-idea`, `design-goal`, `execute-codex-goal`, `diagnose`, `close-goal`, and `maintain-agent-harness`. The five project/design/execute/close/maintain consumers share the active v3 implementation and supported schema cohort; mixed versions fail before mutation.

Other public domain Skills remain independently self-contained. `legacy-skills/` is non-discoverable preservation storage. `authoring/public-resource-manifest.json` and the copy bundled in `maintain-agent-harness` define distribution integrity.
