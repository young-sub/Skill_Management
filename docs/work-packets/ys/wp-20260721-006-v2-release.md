---
title: "WP-20260721-006: Harness V2 Documentation And Formal Release"
status: in-progress
labels:
  - release
  - documentation
  - ci
created_at: 2026-07-21
---

# WP-20260721-006: Harness V2 Documentation And Formal Release

## Korean Summary (non-normative)

### 목적

처음 설치하는 사용자가 요구 도구와 설치 후 실제 실행 경로를 바로 이해할 수 있게 하고, 실패 중인 배포 CI를 고친 뒤 검증된 Harness V2를 정식 `v2.0.0` GitHub Release로 발행한다.

### 핵심 구현 사항

README에 prerequisites와 Codex/Claude Code별 bootstrap 경로를 추가한다. Windows CI에서 사용할 수 없는 `Get-FileHash` 의존성을 portable SHA-256 구현으로 교체하고, release metadata와 release notes를 final 상태로 전환한 뒤 모든 검증과 원격 CI가 통과한 commit에만 `v2.0.0` tag를 생성한다.

> This Korean summary is for review speed only. If it conflicts with the English canonical sections or repository rules, those canonical sources prevail.

## Metadata

- Source: user request on 2026-07-21
- Tracker: local markdown
- Durable tracker after issue: this file
- Intake mode: docs_grill_preflight / diagnose_first for CI
- Intent confidence: 0.98
- Status: ready-for-agent
- Parallel-safe init: yes
- Isolated write surface: this Work Packet

## Phase handoff capsule

- updated_at: 2026-07-21
- source_ref: this Work Packet
- updated_by: Codex main session
- phase: verification
- scope: installation documentation, PowerShell CI portability, and formal v2.0.0 publication
- current gate: local verification and independent review passed; remote CI is required before tagging
- accepted decisions: release version v2.0.0; no Issue/PR publication; tag only after green CI
- open decisions: none
- files read: README, release candidate metadata/tests, setup skill, workflow, CI run 29753965761
- files changed: README, release metadata and notes, PowerShell hash helper/callers, setup Skill contract, resource manifests, regression tests, and this Work Packet
- tracker/PR/doc mutations: local Work Packet; future Git tag and GitHub Release explicitly authorized
- tracker_channel: none
- git_publish_state: release_branch_local
- tracker_publish_state: local_pending
- published_body_ref:
- verification evidence: 128/128 full suite passed; distribution 42/42 and release 20/20 focused suites passed; resource sync, distribution, evidence, diff, and approved 18-Skill Codex/Claude install smoke passed
- delegated evidence: independent read-only reviewer `/root/merge_challenge_ops` returned PASS with no material findings; prospective live metadata remains gated on exact-commit green CI and tag verification
- risks: release tags are externally visible and must not be moved; publication remains blocked until the exact main commit passes GitHub Actions
- next mode: publish
- next stop condition: remote CI failure or tag/release identity mismatch

## Context-budget and delegation plan

- Known from prompt: improve installation docs and publish the formal version
- Must verify from repo: release version, tag/release absence, CI status, installed helper paths
- Do not read yet: archived implementation history not needed for this bounded release
- Direct reads used: README, setup skill, release metadata/tests, tracker/workflow config, CI log
- Orientation searches used: release surfaces and installation path references
- Delegation candidates: final read-only release review
- Delegation used or unavailable: available; deferred until implementation is verified
- Retained working summary: v2.0.0 is the existing candidate; no formal tag/release exists; CI fails because Get-FileHash is unavailable on the runner; README post-install helper path is ambiguous.

## Goal

Publish a documented, CI-green, immutable `v2.0.0` release from the verified Harness V2 implementation.

## Non-goals

- No new public Skill or workflow behavior.
- No tracker-channel migration or GitHub Issue/PR creation.
- No force-push, tag movement, or deletion of historical smoke tags.

## Source of truth

- `README.md`
- `distribution/release-candidate.json`
- `.github/workflows/validate-distribution.yml`
- `skills/setup-agent-harness/SKILL.md`
- `tests/release/test_release_candidate.py`

## Implementation Contract

- Purpose: make installation executable by first-time users and convert the verified candidate into a formal release.
- Core implementation target: portable hash verification, correct installed helper paths, prerequisites, release notes, and live release metadata.
- Observable changes: green GitHub Actions, `v2.0.0` tag, and a published GitHub Release.
- What will not change: the 18-skill catalog and Harness V2 functional contracts.
- Verification signal: focused RED/GREEN tests, 124+ full tests, resource/distribution/evidence checks, green main CI, and remote release/tag inspection.

## Scoped Overrides

- None.

## Proposed Shared Doc Updates

- `README.md`: prerequisites, installed helper paths, and final release wording; apply during run.
- `distribution/release-candidate.json`: final/live release state; apply during run.
- `docs/releases/v2.0.0.md`: durable release notes; create during run.

## Grill / intent alignment

- Full grill decision: skipped; repo evidence fixes version, scope, and release gates.
- Docs/code evidence inspected: README, setup skill, candidate metadata, release tests, workflow and failed CI logs.
- Auto-closed terminology decisions: formal version means existing candidate `v2.0.0`; GitHub Release accompanies immutable tag.
- Blocking questions: none.

## Implementation Confirmation Brief

### Purpose

Turn the already accepted Harness V2 candidate into a usable and formally published release.

### Core implementation target

Fix the observed CI portability failure, correct installation guidance, finalize release metadata and notes, then publish only after green verification.

### Observable changes

- README commands work from an installed target project.
- CI passes on GitHub's Windows runner.
- GitHub exposes an immutable `v2.0.0` tag and Release.

### What will not change

- Public catalog membership and skill behavior remain unchanged.

### Verification signal

- Local and remote checks pass against the release commit.

## Confirmation status

- Status: confirmed
- Evidence: user requested the required documentation work and formal publication on 2026-07-21.

## Decisions made

- Release from `main` only after CI is green.
- Keep tracker publication disabled; use SSH/GitHub release surfaces only.
- Preserve release-smoke tags as historical evidence.

## Vertical slices

1. Reproduce and fix PowerShell hash portability.
2. Correct prerequisites and installed bootstrap paths in README/Skill docs.
3. Finalize release metadata and release notes.
4. Verify, integrate, tag, publish, and inspect the live release.

## Acceptance criteria

- No production PowerShell script depends on `Get-FileHash`.
- README documents Node/npm/npx, Python, and PowerShell expectations plus Codex/Claude installed helper paths.
- Candidate metadata is final/live and identifies `v2.0.0`.
- Full local checks and GitHub Actions pass before tag creation.
- `gh release view v2.0.0` confirms the published non-draft, non-prerelease release.

## Verification plan

- `python -m unittest discover -s tests -p "test_*.py"`
- `powershell -NoProfile -File scripts/sync-skill-resources.ps1 -Check`
- `powershell -NoProfile -File scripts/validate-distribution.ps1`
- `python scripts/validate_evidence.py --repository-root .`
- `git diff --check`
- GitHub Actions `Validate distribution`

## Architecture review target

- Hash helper duplication and consistent use across PowerShell entrypoints.
- Installed-path guidance remains provider-correct and self-contained.
- Release metadata distinguishes evidence source revision from release commit.

## Risks and approval boundaries

- Tag and GitHub Release publication are irreversible external actions; this Work Packet records the user's explicit authorization.
- Stop before tag creation if CI is not green.

## Codex goal outline

Complete the four slices, verify locally and independently, integrate to main, wait for green CI, then publish `v2.0.0`.

## Close report skeleton

- Final refs and release URL
- Verification and CI evidence
- Documentation changes
- Remaining risks

## Durable record notes

- Keep active until the live release is verified; archive only after close.
